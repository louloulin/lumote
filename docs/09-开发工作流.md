# 开发工作流

## 开发环境搭建

### 系统要求
```bash
# 必需软件版本要求
Bun >= 1.2.8        # 主要运行时
Node.js >= 18.0.0    # 兼容性需要  
PostgreSQL >= 13.0   # 数据库
Git >= 2.30.0        # 版本控制
```

### 快速搭建指南
```bash
# 1. 安装 Bun 运行时
curl -fsSL https://bun.sh/install | bash
source ~/.bashrc  # 或重启终端

# 2. 克隆项目仓库
git clone https://github.com/blinko-space/blinko.git
cd blinko

# 3. 安装项目依赖
bun install

# 4. 设置数据库
# macOS
brew install postgresql
brew services start postgresql
createdb blinko_dev

# Ubuntu/Debian  
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo -u postgres createdb blinko_dev

# 5. 配置环境变量
cp app/.env.example app/.env.local
cp server/.env.example server/.env

# 6. 数据库迁移
cd server
bun run prisma:migrate

# 7. 启动开发服务
bun run dev
```

### Docker 开发环境
```yaml
# docker-compose.dev.yml
version: '3.8'
services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: blinko_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev:/var/lib/postgresql/data
      
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_dev:/data

volumes:
  postgres_dev:
  redis_dev:
```

```bash
# 启动开发环境
docker-compose -f docker-compose.dev.yml up -d

# 停止开发环境
docker-compose -f docker-compose.dev.yml down
```

## 项目结构详解

### Monorepo 架构
```
blinko/
├── package.json              # 根包配置
├── turbo.json               # Turborepo 配置
├── tsconfig.json            # TypeScript 根配置
├── bun.lockb                # Bun 锁文件
│
├── app/                     # 前端应用 (Next.js + React)
│   ├── src/
│   │   ├── components/      # React 组件
│   │   │   ├── ui/         # 基础 UI 组件
│   │   │   ├── layout/     # 布局组件
│   │   │   └── features/   # 功能组件
│   │   ├── pages/          # Next.js 页面
│   │   ├── store/          # Zustand 状态管理
│   │   │   ├── user/       # 用户状态
│   │   │   ├── note/       # 笔记状态
│   │   │   └── plugin/     # 插件状态
│   │   ├── lib/            # 工具函数
│   │   │   ├── api/        # API 客户端
│   │   │   ├── utils/      # 通用工具
│   │   │   └── hooks/      # React Hooks
│   │   └── styles/         # 样式文件
│   ├── public/             # 静态资源
│   └── package.json        # 前端依赖
│
├── server/                  # 后端服务 (Bun + Express)
│   ├── routerTrpc/         # tRPC 路由
│   │   ├── user.ts         # 用户相关 API
│   │   ├── note.ts         # 笔记相关 API
│   │   ├── ai.ts           # AI 功能 API
│   │   └── plugin.ts       # 插件系统 API
│   ├── routerExpress/      # Express 路由
│   ├── aiServer/           # AI 服务
│   │   ├── vector/         # 向量数据库
│   │   ├── rag/            # RAG 系统
│   │   └── providers/      # AI 提供商
│   ├── lib/                # 业务逻辑
│   ├── middleware/         # 中间件
│   ├── types/              # 类型定义
│   └── package.json        # 后端依赖
│
├── shared/                  # 共享代码
│   ├── lib/
│   │   ├── types.ts        # 共享类型
│   │   ├── utils.ts        # 共享工具
│   │   └── constants.ts    # 常量定义
│   └── package.json
│
├── prisma/                  # 数据库
│   ├── schema.prisma       # 数据模型
│   ├── migrations/         # 数据库迁移
│   └── seed.ts             # 种子数据
│
├── desktop/                 # Tauri 桌面应用
│   ├── src-tauri/          # Rust 后端
│   └── package.json
│
└── docs/                    # 项目文档
    ├── README.md           # 文档索引
    ├── 01-项目概述.md      # 项目介绍
    └── ...                 # 其他文档
```

### 代码组织原则
```typescript
// 功能模块组织示例
interface ModuleStructure {
  // 按功能分组
  byFeature: {
    notes: {
      components: "笔记相关组件",
      store: "笔记状态管理", 
      api: "笔记 API 接口",
      types: "笔记类型定义"
    };
  };
  
  // 按层次分组
  byLayer: {
    presentation: "UI 组件层",
    business: "业务逻辑层",
    data: "数据访问层",
    infrastructure: "基础设施层"
  };
  
  // 共享模块
  shared: {
    types: "通用类型",
    utils: "工具函数",
    components: "复用组件",
    hooks: "自定义 Hooks"
  };
}
```

## 开发工具配置

### VSCode 配置
```json
// .vscode/settings.json
{
  "typescript.preferences.importModuleSpecifier": "relative",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true,
    "source.organizeImports": true
  },
  "files.associations": {
    "*.css": "tailwindcss"
  },
  "tailwindCSS.includeLanguages": {
    "typescript": "javascript",
    "typescriptreact": "javascript"
  },
  "eslint.workingDirectories": [
    "app",
    "server", 
    "shared"
  ]
}
```

### ESLint 配置
```javascript
// .eslintrc.js
module.exports = {
  root: true,
  extends: [
    '@next/eslint-config-next',
    '@typescript-eslint/recommended',
    'prettier'
  ],
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint'],
  rules: {
    '@typescript-eslint/no-unused-vars': 'error',
    '@typescript-eslint/explicit-function-return-type': 'warn',
    'import/order': [
      'error',
      {
        groups: [
          'builtin',
          'external', 
          'internal',
          'parent',
          'sibling',
          'index'
        ],
        'newlines-between': 'always'
      }
    ]
  },
  overrides: [
    {
      files: ['app/**/*'],
      extends: ['next/core-web-vitals']
    },
    {
      files: ['server/**/*'],
      env: {
        node: true
      }
    }
  ]
};
```

### Prettier 配置
```json
// .prettierrc
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2,
  "useTabs": false,
  "bracketSpacing": true,
  "arrowParens": "avoid",
  "endOfLine": "lf"
}
```

## 开发脚本

### 根目录脚本
```json
// package.json scripts
{
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules",
    
    // 数据库操作
    "db:generate": "cd server && bun run prisma:generate",
    "db:migrate": "cd server && bun run prisma:migrate", 
    "db:studio": "cd server && bun run prisma:studio",
    "db:seed": "cd server && bun run prisma:seed",
    "db:reset": "cd server && bun run prisma:reset",
    
    // 部署相关
    "docker:build": "docker build -t blinko .",
    "docker:run": "docker-compose up -d",
    "docker:stop": "docker-compose down",
    
    // 代码质量
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:fix": "turbo run lint -- --fix"
  }
}
```

### 前端脚本
```json
// app/package.json scripts  
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  }
}
```

### 后端脚本
```json
// server/package.json scripts
{
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    
    // Prisma 相关
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:seed": "bun run prisma/seed.ts",
    "prisma:reset": "prisma migrate reset --force"
  }
}
```

## 调试技巧

### 前端调试
```typescript
// 调试 Hook 示例
import { useEffect } from 'react';

const useDebugger = (value: any, name: string) => {
  useEffect(() => {
    console.log(`[${name}] 值变化:`, value);
  }, [value, name]);
};

// 组件调试
const MyComponent = () => {
  const [state, setState] = useState(initialState);
  
  // 调试状态变化
  useDebugger(state, 'MyComponent-state');
  
  return <div>...</div>;
};
```

### 后端调试
```typescript
// 调试中间件
const debugMiddleware = (req: Request, res: Response, next: NextFunction) => {
  console.log(`[${new Date().toISOString()}] ${req.method} ${req.path}`);
  console.log('Headers:', req.headers);
  console.log('Body:', req.body);
  
  const start = Date.now();
  res.on('finish', () => {
    const duration = Date.now() - start;
    console.log(`[${req.method} ${req.path}] ${res.statusCode} - ${duration}ms`);
  });
  
  next();
};

// tRPC 调试
const createTRPCContext = ({ req, res }: CreateContextOptions) => {
  console.log('[tRPC] 请求:', req.url);
  return {
    req,
    res,
    prisma,
    // ... other context
  };
};
```

### 数据库调试
```typescript
// Prisma 查询日志
const prisma = new PrismaClient({
  log: [
    {
      emit: 'event',
      level: 'query'
    },
    {
      emit: 'stdout',
      level: 'error'
    },
    {
      emit: 'stdout', 
      level: 'warn'
    }
  ]
});

prisma.$on('query', (e) => {
  console.log('Query:', e.query);
  console.log('Params:', e.params);
  console.log('Duration:', e.duration + 'ms');
});
```

## 测试策略

### 单元测试
```typescript
// 工具函数测试
import { describe, it, expect } from 'vitest';
import { formatDate, calculateReadingTime } from '../utils';

describe('工具函数测试', () => {
  it('格式化日期', () => {
    const date = new Date('2024-01-01');
    expect(formatDate(date, 'YYYY-MM-DD')).toBe('2024-01-01');
  });
  
  it('计算阅读时间', () => {
    const text = '这是一个测试文本'.repeat(50);
    const readingTime = calculateReadingTime(text);
    expect(readingTime).toBeGreaterThan(0);
  });
});
```

### 组件测试
```typescript
// React 组件测试
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import NoteCard from '../NoteCard';

describe('NoteCard 组件', () => {
  const mockNote = {
    id: '1',
    title: '测试笔记',
    content: '这是测试内容',
    createdAt: new Date()
  };
  
  it('渲染笔记信息', () => {
    render(<NoteCard note={mockNote} />);
    
    expect(screen.getByText('测试笔记')).toBeInTheDocument();
    expect(screen.getByText('这是测试内容')).toBeInTheDocument();
  });
  
  it('点击事件触发', () => {
    const onClickMock = vi.fn();
    render(<NoteCard note={mockNote} onClick={onClickMock} />);
    
    fireEvent.click(screen.getByRole('button'));
    expect(onClickMock).toHaveBeenCalledWith(mockNote);
  });
});
```

### API 测试
```typescript
// tRPC API 测试
import { describe, it, expect } from 'vitest';
import { createCallerFactory } from '@trpc/server';
import { appRouter } from '../router';

describe('笔记 API 测试', () => {
  const createCaller = createCallerFactory(appRouter);
  const caller = createCaller({
    // mock context
  });
  
  it('创建笔记', async () => {
    const input = {
      title: '测试笔记',
      content: '测试内容'
    };
    
    const result = await caller.note.create(input);
    
    expect(result.title).toBe(input.title);
    expect(result.content).toBe(input.content);
    expect(result.id).toBeDefined();
  });
  
  it('获取笔记列表', async () => {
    const result = await caller.note.list({
      limit: 10,
      offset: 0
    });
    
    expect(Array.isArray(result.notes)).toBe(true);
    expect(typeof result.total).toBe('number');
  });
});
```

### 集成测试
```typescript
// 端到端测试 (Playwright)
import { test, expect } from '@playwright/test';

test.describe('笔记功能集成测试', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    // 登录流程
    await page.fill('[data-testid=email]', 'test@example.com');
    await page.fill('[data-testid=password]', 'password');
    await page.click('[data-testid=login-button]');
  });
  
  test('创建和编辑笔记', async ({ page }) => {
    // 创建笔记
    await page.click('[data-testid=new-note-button]');
    await page.fill('[data-testid=note-title]', '测试笔记标题');
    await page.fill('[data-testid=note-content]', '测试笔记内容');
    await page.click('[data-testid=save-button]');
    
    // 验证笔记已保存
    await expect(page.locator('[data-testid=note-list]')).toContainText('测试笔记标题');
    
    // 编辑笔记
    await page.click('[data-testid=note-item]:first-child');
    await page.fill('[data-testid=note-title]', '修改后的标题');
    await page.click('[data-testid=save-button]');
    
    // 验证修改已保存
    await expect(page.locator('[data-testid=note-list]')).toContainText('修改后的标题');
  });
});
```

## 性能优化

### 代码分割
```typescript
// 动态导入和懒加载
import { lazy, Suspense } from 'react';
import LoadingSpinner from './LoadingSpinner';

// 懒加载大型组件
const NoteEditor = lazy(() => import('./NoteEditor'));
const PluginManager = lazy(() => import('./PluginManager'));

const App = () => {
  return (
    <Suspense fallback={<LoadingSpinner />}>
      <Routes>
        <Route path="/editor" element={<NoteEditor />} />
        <Route path="/plugins" element={<PluginManager />} />
      </Routes>
    </Suspense>
  );
};
```

### 缓存策略
```typescript
// React Query 缓存配置
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5分钟
      gcTime: 10 * 60 * 1000, // 10分钟
      retry: 1,
      refetchOnWindowFocus: false
    }
  }
});

// 缓存Hook
const useNotes = (filters: NoteFilters) => {
  return useQuery({
    queryKey: ['notes', filters],
    queryFn: () => api.note.list(filters),
    staleTime: 2 * 60 * 1000 // 笔记列表缓存2分钟
  });
};
```

### 构建优化
```javascript
// next.config.js
const nextConfig = {
  // 编译优化
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production'
  },
  
  // 实验性功能
  experimental: {
    turbo: true,
    serverComponentsExternalPackages: ['prisma']
  },
  
  // 图片优化
  images: {
    domains: ['localhost'],
    formats: ['image/webp', 'image/avif']
  },
  
  // Webpack 优化
  webpack: (config, { dev, isServer }) => {
    if (!dev && !isServer) {
      // 生产环境优化
      config.optimization.splitChunks.chunks = 'all';
    }
    return config;
  }
};
```

## Git 工作流

### 分支策略
```bash
# 主要分支
main      # 生产环境分支
develop   # 开发环境分支

# 功能分支
feature/user-auth           # 用户认证功能
feature/plugin-system       # 插件系统
feature/ai-integration     # AI 集成

# 修复分支  
hotfix/security-patch       # 安全补丁
bugfix/note-delete-issue    # Bug 修复

# 发布分支
release/v1.0.0             # 版本发布
```

### 提交规范
```bash
# 提交类型
feat:     新功能
fix:      Bug 修复  
docs:     文档更新
style:    代码格式调整
refactor: 代码重构
test:     测试相关
chore:    构建过程或辅助工具的变动

# 提交示例
git commit -m "feat(auth): 添加用户登录功能"
git commit -m "fix(note): 修复笔记删除时的并发问题"  
git commit -m "docs(api): 更新 API 文档"
git commit -m "test(component): 添加 NoteCard 组件测试"
```

### 代码审查清单
```markdown
## 代码审查检查项

### 功能性
- [ ] 功能是否按需求正确实现
- [ ] 是否考虑了边界情况
- [ ] 错误处理是否完善
- [ ] 性能是否可接受

### 代码质量
- [ ] 代码是否易读易懂
- [ ] 变量和函数命名是否清晰
- [ ] 是否遵循项目编码规范
- [ ] 是否有适当的注释

### 测试
- [ ] 是否包含单元测试
- [ ] 测试覆盖率是否足够
- [ ] 是否有集成测试

### 安全性
- [ ] 是否存在安全漏洞
- [ ] 输入验证是否充分
- [ ] 敏感信息是否妥善处理
```

---

*通过规范化的开发工作流，Blinko 项目能够保持高质量的代码和稳定的开发节奏，为团队协作和产品迭代提供坚实的基础。*
